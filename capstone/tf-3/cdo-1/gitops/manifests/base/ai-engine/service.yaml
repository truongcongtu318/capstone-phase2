apiVersion: v1
kind: Service
metadata:
  name: ai-engine
  labels:
    app: ai-engine
spec:
  type: ClusterIP
  selector:
    app: ai-engine
  ports:
    - port: 8080
      targetPort: 80 # TODO: đổi thành 8080 khi container thật lắng nghe đúng cổng
      protocol: TCP
