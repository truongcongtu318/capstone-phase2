# ============================================================
# serviceaccount.yaml - ai-engine
# Sở hữu: Member 7
#
# TUYỆT ĐỐI KHÔNG gắn ClusterRole/Role nào cho ServiceAccount này.
# AI Engine không cần quyền K8s API, chỉ cần gọi Bedrock qua IRSA.
# TODO khi lên EKS thật: thêm annotation IRSA role gọi Bedrock.
# ============================================================
apiVersion: v1
kind: ServiceAccount
metadata:
  name: ai-engine
  labels:
    app: ai-engine
  # annotations:
  #   eks.amazonaws.com/role-arn: arn:aws:iam::544011261607:role/<irsa-ai-engine-bedrock>
