apiVersion: v1
kind: Service
metadata:
  name: sqs-worker
  labels:
    app: sqs-worker
  annotations:
    argocd.argoproj.io/sync-wave: "-1"
spec:
  type: ClusterIP
  selector:
    app: sqs-worker
  ports:
    - name: metrics
      port: 9090
      targetPort: 9090
      protocol: TCP
