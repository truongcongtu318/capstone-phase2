apiVersion: v1
kind: Service
metadata:
  name: sqs-worker
  labels:
    app: sqs-worker
  annotations:
    argocd.argoproj.io/sync-wave: "-1"
spec:
  type: ClusterIP
  selector:
    app: sqs-worker
  ports:
    - name: metrics
      port: 8080
      targetPort: 8080
      protocol: TCP
