# ============================================================
# serviceaccount.yaml - sqs-worker
# Map với "sa/patch-controller": get,list,watch,patch,update trên
# deployments/statefulsets/configmaps/hpa, CHỈ trong namespace
# tenant-payment, tenant-checkout.
#
# TODO khi lên EKS thật: thêm annotation IRSA:
#   eks.amazonaws.com/role-arn: arn:aws:iam::544011261607:role/<irsa-patch-controller>
# Trên Kind local, annotation IRSA không có hiệu lực (an toàn bỏ qua).
# ============================================================
apiVersion: v1
kind: ServiceAccount
metadata:
  name: sqs-worker
  labels:
    app: sqs-worker
  # annotations:
  #   eks.amazonaws.com/role-arn: arn:aws:iam::544011261607:role/<irsa-patch-controller>
