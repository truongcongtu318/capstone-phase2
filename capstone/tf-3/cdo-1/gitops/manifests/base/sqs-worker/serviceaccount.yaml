# ============================================================
# serviceaccount.yaml - sqs-worker
# Sở hữu: Member 7
#
# Đây là ServiceAccount map với "sa/patch-controller" trong project-rules
# (get,list,watch,patch,update trên deployments/statefulsets/configmaps/hpa,
# CHỈ trong namespace tenant-payment, tenant-checkout).
#
# TODO khi lên EKS thật: thêm annotation IRSA:
#   eks.amazonaws.com/role-arn: arn:aws:iam::544011261607:role/<irsa-patch-controller>
# Trên Kind local, annotation IRSA không có hiệu lực (an toàn để bỏ qua).
# ============================================================
apiVersion: v1
kind: ServiceAccount
metadata:
  name: self-heal-executor
  labels:
    app: sqs-worker
  # annotations:
  #   eks.amazonaws.com/role-arn: arn:aws:iam::544011261607:role/<irsa-patch-controller>
