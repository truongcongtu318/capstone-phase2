apiVersion: v1
kind: Service
metadata:
  name: order-service
  namespace: tenant-payment
  labels:
    app: order-service
spec:
  type: ClusterIP
  selector:
    app: order-service
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      protocol: TCP
