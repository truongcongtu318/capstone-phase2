# ============================================================
# serviceaccount.yaml - webhook-receiver
# ServiceAccount chính thức: webhook-receiver
# Không gắn RBAC patch quyền — webhook chỉ đọc (get/list) ConfigMap/Service.
# Tên đồng bộ với deployment.yaml và networkpolicy selector.
# ============================================================
apiVersion: v1
kind: ServiceAccount
metadata:
  name: webhook-receiver
  labels:
    app: webhook-receiver
