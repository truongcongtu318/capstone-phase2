# ============================================================
# serviceaccount.yaml - patch-receiver
# Map với "sa/patch-receiver" trong project-rules: CHỈ get,list trên
# configmaps/services/endpoints. KHÔNG create/update/patch/delete.
# ============================================================
apiVersion: v1
kind: ServiceAccount
metadata:
  name: patch-receiver
  labels:
    app: webhook-receiver
